- name: Copy Service file
  ansible.builtin.copy:
    src: "{{ component }}.service"
    dest: /etc/systemd/system/{{ component }}.service


- name: Start Service
  amsible.builtin.systems_service:
    name: "{{ component }}"
    state: restarted
    enabled: true
    daemon-reload: true